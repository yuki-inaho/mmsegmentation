# Local cu12 GPU smoke result (2026-05-28)

This fork branch (`cu121-mmseg122-gpu-validation-may28`) was used by the parent
sandbox `mmopenlab_cu12_sandbox` to verify cu12 GPU operation on a local box.

| field | value |
| :-- | :-- |
| date (UTC) | 2026-05-28 03:36:36 |
| GPU | NVIDIA RTX 2000 Ada Generation (sm_89, compute capability 8.9) |
| Driver | 550.127.08 (CUDA 12.4 runtime) |
| torch | 2.1.0+cu121 |
| mmcv | 2.1.0 (cu121 wheel) |
| mmseg | 1.2.2 (PyPI; pure-python) |
| mmengine | 0.10.x |
| CUDA op | `mmseg.models.decode_heads.FCNHead` forward on `device='cuda'` |
| op output device | `cuda` |
| op output shape | `[1, 2, 16, 16]`, dtype `torch.float32` |
| result | **success** |

## Required change candidates surfaced (vs. fork main `mmseg 1.2.2`)

- `mmseg/__init__.py:10-11`
  - Current: `MMCV_MIN = '2.0.0rc4'`, `MMCV_MAX = '2.2.0'` (exclusive `<`).
  - Smoke: works with `mmcv 2.1.0`. No fork patch was needed.
  - Future: if `mmcv 2.2.0` is required, relax to `<=2.2.0` or higher.
- `mmseg/__init__.py:12-13`
  - Current: mmengine `>=0.5.0`, `<1.0.0`. mmengine 0.10.x within bounds.
- STDC2 RGB-D smoke is NOT covered here (the RGB-D pipeline lives in the
  downstream repo `tomato_stem_mmsegmentation`, not in this fork).

Evidence (parent repo):
- `06_execution_logs/smoke_mmseg_20260528.json`

Workdoc / acceptance (parent repo):
- `temp/workdoc_May28-2026_cu12_submodule_gpu_validation.md`
- `temp/acceptance_May28-2026_cu12_submodule_gpu_validation.md`
